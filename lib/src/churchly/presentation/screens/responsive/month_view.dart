import 'package:flutter/material.dart';

class DMonthView extends StatelessWidget {
  const DMonthView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}